import React from 'react'

const ResultsSection = ({ children }) => {
    return (
        <div class="results">
          <h3>Results</h3>
          {children}
        </div>
    )
}

export default ResultsSection
